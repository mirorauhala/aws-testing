{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "fastify start -l info --env production server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.9.2",
    "fastify-cli": "^5.5.1"
  }
}
